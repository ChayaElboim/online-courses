export interface User {
  id?: number; // סימן השאלה מציין שזה יכול להיות אופציונלי (למשל, ה-Backend יכול ליצור את ה-ID)
    name: string;
    email: string;
    password?: string; // סיסמה תהיה קיימת בזמן רישום או התחברות, אבל לא תמיד כשמקבלים מידע על משתמש
    role: 'student' | 'teacher'; // מגדירים את סוגי התפקידים האפשריים
    // את יכולה להוסיף כאן שדות נוספים כמו תאריך יצירה, תאריך עדכון, וכו'
  }